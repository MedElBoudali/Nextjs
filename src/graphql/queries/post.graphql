query GetPost($id: Int!) {
  getPost(id: $id) {
    id
    title
    text
    points
    authorId
    voteStatus
    author {
      id
      username
      email
    }
    createdAt
    updatedAt
    textSnippet
  }
}
