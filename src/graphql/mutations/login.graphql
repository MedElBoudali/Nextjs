mutation Login($username: String!, $password: String!) {
  login(userIputs: { username: $username, password: $password }) {
    errors {
      field
      message
    }
    user {
      id
      username
    }
  }
}
